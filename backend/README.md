# Nexus backend (demo contract)

Run after installing requirements:

```bash
python -m uvicorn app.main:app --reload --port 8000
```

This is deliberately a hardcoded FastAPI contract for the hackathon demo. Database, AI, YouTube, and secure auth integrations are deferred until credentials are approved.
